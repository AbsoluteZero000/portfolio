<div class="cmd-nav">
  <button class="cmd-chip" data-cmd="ls projects/">projects</button>
  <button class="cmd-chip" data-cmd="cat experience">experience</button>
  <button class="cmd-chip" data-cmd="cat skills">skills</button>
  <button class="cmd-chip" data-cmd="whoami">about</button>
  <button class="cmd-chip" data-cmd="contact">contact</button>
</div>

<style>
  .cmd-nav {
    display: flex;
    gap: 8px;
    padding: 0 20px 8px;
    flex-shrink: 0;
    flex-wrap: wrap;
  }

  .cmd-chip {
    background: var(--bg-dim);
    border: 1px solid var(--border);
    color: var(--accent);
    font-family: inherit;
    font-size: 12px;
    padding: 4px 12px;
    border-radius: 4px;
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s;
  }

  .cmd-chip:hover {
    background: var(--border);
    border-color: var(--accent-dim);
  }

  .cmd-chip:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }
</style>
